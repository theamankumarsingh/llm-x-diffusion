[project]
name = "llm-x-diffusion"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aman Kumar Singh", email = "amankumarsingh.professional@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.14.0",
    "diffusers>=0.36.0",
    "python-dotenv>=1.2.3",
    "safetensors>=0.8.0",
    "torch>=2.14.0",
    "transformers>=5.16.1",
]

[project.scripts]
llm-x-diffusion = "llm_x_diffusion.main:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
